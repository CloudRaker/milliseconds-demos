import type { DemoMeta } from '../meta';
export default { slug:'receipt-boxes', title:'Receipt Boxes', summary:'Extract a receipt record straight from the image and see the box each value was read from.', instruction:'Run the stock receipt free, then drop in your own image with your key.', routes:['extract'], category:'Documents & operations', order:2 } satisfies DemoMeta;
