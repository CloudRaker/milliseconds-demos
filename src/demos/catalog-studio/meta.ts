import type { DemoMeta } from '../meta';
export default {
  slug: 'catalog-studio', title: 'Catalog Studio',
  summary: "Messy product copy. Clean catalog fields.",
  instruction: "Structure the catalog, then inspect a listing.",
  routes: ['classify', 'extract'], category: 'Sales & commerce', order: 3,
} satisfies DemoMeta;
