import type { DemoMeta } from '../meta';
export default { slug:'receipt-reader', title:'Receipt Reader', summary: "A receipt photo becomes a structured record.", instruction: "Run the receipt, or drop in your own image.", routes:['extract'], modality:'image', category:'Documents & operations', order:2 } satisfies DemoMeta;
