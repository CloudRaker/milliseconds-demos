import type { DemoMeta } from '../meta';
export default { slug:'receipt-reader', title:'Receipt Reader', summary:'Extract a receipt record straight from the image, with no parsing step in between.', instruction:'Run the stock receipt free, then drop in your own image with your key.', routes:['extract'], category:'Documents & operations', order:2 } satisfies DemoMeta;
