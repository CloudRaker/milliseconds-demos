import type { DemoMeta } from '../meta';
export default {slug:'sales-intake',title:'Sales Intake',summary: "Sales lead or support ticket? Send it to the right team.",instruction: "Run the inbox, then open a message.",routes:['yes-no','classify','extract'],category:'Sales & commerce',order:2} satisfies DemoMeta;
